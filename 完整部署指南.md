# 橙果宜牛系统完整部署指南

## 📋 概述

本指南提供将橙果宜牛系统部署到全新服务器的完整步骤。系统包含前端Vue应用、后端Node.js API服务、数据库、Nginx反向代理等组件。

## 🖥️ 服务器要求

### 最低配置
- **操作系统**: Ubuntu 20.04 LTS 或 CentOS 7+
- **CPU**: 1核
- **内存**: 1GB
- **磁盘**: 20GB
- **网络**: 1Mbps带宽

### 推荐配置
- **操作系统**: Ubuntu 22.04 LTS
- **CPU**: 2核
- **内存**: 2GB
- **磁盘**: 50GB SSD
- **网络**: 3Mbps带宽

## 📦 必需软件

### 系统软件
```bash
# 更新系统
sudo apt update && sudo apt upgrade -y

# 安装基础工具
sudo apt install -y curl wget git unzip software-properties-common

# 安装Node.js 18+
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

# 安装Nginx
sudo apt install -y nginx

# 安装PM2进程管理器
sudo npm install -g pm2

# 安装SQLite3 (如果使用SQLite数据库)
sudo apt install -y sqlite3

# 安装Python (用于certbot)
sudo apt install -y python3 python3-pip

# 安装Let's Encrypt certbot
sudo apt install -y certbot python3-certbot-nginx
```

### 验证安装
```bash
# 检查版本
node --version    # v18.x.x
npm --version     # 9.x.x
nginx -v         # nginx/1.18.x
pm2 --version    # 5.x.x
sqlite3 --version # 3.x.x
```

## 🔐 SSH配置

### 1. 生成SSH密钥对
```bash
# 在本地机器上生成密钥
ssh-keygen -t rsa -b 4096 -C "your-email@example.com" -f ~/.ssh/id_rsa_deploy

# 复制公钥到服务器
ssh-copy-id -i ~/.ssh/id_rsa_deploy.pub root@YOUR_SERVER_IP
```

### 2. 配置SSH客户端
在本地 `~/.ssh/config` 文件中添加：
```
Host chengguo-server
    HostName YOUR_SERVER_IP
    User root
    IdentityFile ~/.ssh/id_rsa_deploy
    IdentitiesOnly yes
```

### 3. 测试连接
```bash
ssh chengguo-server
```

## 📁 项目部署

### 1. 创建项目目录
```bash
# 在服务器上执行
sudo mkdir -p /var/www
cd /var/www
```

### 2. 克隆项目代码
```bash
# 从Git仓库克隆 (如果有的话)
git clone YOUR_REPOSITORY_URL douyin-admin-master

# 或者直接上传代码
# 在本地执行
scp -r ./ root@YOUR_SERVER_IP:/var/www/douyin-admin-master
```

### 3. 设置目录权限
```bash
sudo chown -R www-data:www-data /var/www
sudo chmod -R 755 /var/www
```

## 🔧 环境配置

### 1. 创建环境变量文件
```bash
cd /var/www/douyin-admin-master
sudo cp .env.example .env  # 如果存在的话，否则手动创建
```

编辑 `.env` 文件：
```bash
# JWT密钥 (必须设置强密钥)
JWT_SECRET=your-super-secure-jwt-secret-key-here

# 服务器端口
PORT=3000

# 数据库配置 (默认SQLite)
DB_TYPE=sqlite

# 抖音API配置
VITE_DOUYIN_APP_ID=your-douyin-app-id
VITE_DOUYIN_APP_SECRET=your-douyin-app-secret

# 环境
NODE_ENV=production
```

### 2. 生成强JWT密钥
```bash
# 生成32字节的随机密钥
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

## 💾 数据库初始化

### 1. 安装依赖
```bash
cd /var/www/douyin-admin-master
npm install --production
```

### 2. 初始化数据库
```bash
# 创建数据库表
node scripts/init-db.js

# 初始化默认token (如果脚本存在)
node scripts/init-tokens.js

# 初始化用户角色 (如果脚本存在)
node scripts/migrate-user-roles.js
```

### 3. 创建管理员用户
```bash
# 使用数据库脚本创建初始管理员
node -e "
const { sequelize } = require('./config/database');
const User = require('./models/User')(sequelize);

async function createAdmin() {
  try {
    await sequelize.sync();
    const admin = await User.createUser({
      username: 'admin',
      password: 'Admin123!',
      name: '系统管理员',
      role: 'admin'
    });
    console.log('管理员创建成功:', admin.username);
    process.exit(0);
  } catch (error) {
    console.error('创建管理员失败:', error);
    process.exit(1);
  }
}
createAdmin();
"
```

## 🌐 Nginx配置

### 1. 创建主应用配置
创建 `/etc/nginx/sites-available/ecpm-app`:
```nginx
server {
    listen 80;
    server_name your-domain.com www.your-domain.com;
    return 301 https://$server_name$request_uri;
}

# HTTPS配置
server {
    listen 443 ssl http2;
    server_name your-domain.com www.your-domain.com;

    # SSL证书路径 (稍后配置)
    ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;

    # SSL安全配置
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;

    root /var/www/html;
    index index.html;

    # SPA前端路由支持
    location / {
        try_files $uri /index.html;
    }

    # API代理到后端服务
    location /api/ {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;

        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # 广告监测代理
    location = /openid/report {
        proxy_pass http://localhost:3000/openid/report;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
        access_log /var/log/nginx/ad-monitor.access.log;
    }

    # ACME challenge目录
    location /.well-known/acme-challenge/ {
        allow all;
    }
}

# IP访问重定向到域名
server {
    listen 80;
    return 301 https://your-domain.com$request_uri;
}
```

### 2. 创建武霸哥配置 (如果需要)
创建 `/etc/nginx/sites-available/nginx-wubug.conf`:
```nginx
server {
    listen 80;
    server_name www.wubug.cc wubug.cc;

    root /var/www/html;
    index index.html index.htm;

    # Gzip压缩
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;

    # SPA前端路由支持
    location / {
        try_files $uri $uri/ /index.html;

        # 缓存静态资源
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
    }

    # API代理到后端服务
    location /api/ {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;

        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # 广告监测代理
    location /openid/report {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
        access_log /var/log/nginx/ad-monitor.access.log;
    }

    # 安全头设置
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;
    add_header Content-Security-Policy "default-src 'self' http: https: data: blob: 'unsafe-inline'" always;

    # 日志配置
    access_log /var/log/nginx/wubug-admin.access.log;
    error_log /var/log/nginx/wubug-admin.error.log;
}
```

### 3. 启用站点配置
```bash
# 启用主应用
sudo ln -s /etc/nginx/sites-available/ecpm-app /etc/nginx/sites-enabled/

# 启用武霸哥配置 (如果需要)
sudo ln -s /etc/nginx/sites-available/nginx-wubug.conf /etc/nginx/sites-enabled/

# 移除默认配置
sudo rm /etc/nginx/sites-enabled/default

# 测试配置
sudo nginx -t

# 重启Nginx
sudo systemctl reload nginx
```

## 🚀 前端部署

### 1. 构建前端
```bash
# 在本地开发环境
npm run build

# 构建产物在 dist/ 目录
```

### 2. 上传前端文件
```bash
# 从本地上传到服务器
scp -r dist/* root@YOUR_SERVER_IP:/var/www/html/
```

### 3. 设置前端文件权限
```bash
sudo chown -R www-data:www-data /var/www/html
sudo chmod -R 755 /var/www/html
```

## ⚙️ 后端服务部署

### 1. 使用PM2启动服务
```bash
cd /var/www/douyin-admin-master

# 创建PM2配置文件
cat > ecosystem.config.js << EOF
module.exports = {
  apps: [{
    name: 'douyin-admin',
    script: 'server.js',
    instances: 1,
    autorestart: true,
    watch: false,
    max_memory_restart: '1G',
    env: {
      NODE_ENV: 'production',
      PORT: 3000
    },
    error_file: '/var/log/pm2/douyin-admin-error.log',
    out_file: '/var/log/pm2/douyin-admin-out.log',
    log_file: '/var/log/pm2/douyin-admin.log'
  }]
};
EOF

# 启动服务
pm2 start ecosystem.config.js

# 设置开机自启
pm2 startup
pm2 save

# 查看状态
pm2 status
pm2 logs douyin-admin
```

### 2. 验证后端服务
```bash
# 测试API
curl http://localhost:3000/api/health

# 检查端口监听
netstat -tlnp | grep :3000
```

## 🔒 SSL证书配置

### 1. 申请Let's Encrypt证书
```bash
# 停止Nginx (certbot需要控制80端口)
sudo systemctl stop nginx

# 申请证书
sudo certbot certonly --standalone -d your-domain.com -d www.your-domain.com

# 启动Nginx
sudo systemctl start nginx
```

### 2. 配置自动续期
```bash
# 测试续期
sudo certbot renew --dry-run

# 查看定时任务
sudo crontab -l | grep certbot
```

## 🔧 部署脚本

### 1. 创建部署脚本
创建 `/var/www/deploy-frontend.sh`:
```bash
#!/bin/bash

echo "🚀 开始前端部署..."

# 备份当前版本
BACKUP_DIR="/var/www/html_backup_$(date +%Y%m%d_%H%M%S)"
sudo cp -r /var/www/html "$BACKUP_DIR"
echo "📦 备份完成: $BACKUP_DIR"

# 清理旧文件
sudo rm -rf /var/www/html/*

# 复制新文件
sudo cp -r /path/to/your/dist/* /var/www/html/

# 设置权限
sudo chown -R www-data:www-data /var/www/html
sudo chmod -R 755 /var/www/html

# 重启Nginx
sudo systemctl reload nginx

echo "✅ 前端部署完成！"
echo "🔄 备份位置: $BACKUP_DIR"
```

### 2. 创建后端部署脚本
创建 `/var/www/deploy-backend.sh`:
```bash
#!/bin/bash

echo "🚀 开始后端部署..."

cd /var/www/douyin-admin-master

# 备份数据库
DB_BACKUP="/var/www/database_backup_$(date +%Y%m%d_%H%M%S).sqlite"
sudo cp database.sqlite "$DB_BACKUP"
echo "💾 数据库备份完成: $DB_BACKUP"

# 安装依赖
npm install --production

# 重启服务
pm2 restart douyin-admin

# 等待服务启动
sleep 5

# 验证服务
if curl -f http://localhost:3000/api/health > /dev/null 2>&1; then
    echo "✅ 后端部署成功！"
    echo "🔄 数据库备份: $DB_BACKUP"
else
    echo "❌ 后端部署失败，请检查日志"
    pm2 logs douyin-admin --lines 20
    exit 1
fi
```

### 3. 设置脚本权限
```bash
sudo chmod +x /var/www/deploy-frontend.sh
sudo chmod +x /var/www/deploy-backend.sh
```

## 📊 监控和维护

### 1. 设置日志轮转
```bash
# 创建日志轮转配置
sudo tee /etc/logrotate.d/douyin-admin << EOF
/var/log/pm2/*.log {
    daily
    missingok
    rotate 7
    compress
    notifempty
    create 0644 www-data www-data
    postrotate
        pm2 reloadLogs
    endscript
}
EOF
```

### 2. 设置监控脚本
创建 `/var/www/monitor.sh`:
```bash
#!/bin/bash

echo "=== 系统监控报告 $(date) ==="

# 检查服务状态
echo "🔍 服务状态:"
pm2 jlist | jq -r '.[] | "\(.name): \(.pm2_env.status)"'

# 检查端口
echo -e "\n🔌 端口监听:"
netstat -tlnp | grep -E ':(80|443|3000)'

# 检查磁盘使用
echo -e "\n💾 磁盘使用:"
df -h /var/www

# 检查内存使用
echo -e "\n🧠 内存使用:"
free -h

# 检查最近的错误日志
echo -e "\n📋 最近错误日志:"
tail -10 /var/log/pm2/douyin-admin-error.log 2>/dev/null || echo "无错误日志"
```

### 3. 设置定时任务
```bash
# 添加到crontab
sudo crontab -e

# 添加以下行：
# 每小时监控一次
0 * * * * /var/www/monitor.sh >> /var/log/system-monitor.log 2>&1

# 每天凌晨备份数据库
0 2 * * * cp /var/www/douyin-admin-master/database.sqlite /var/www/database_daily_$(date +\%Y\%m\%d).sqlite && find /var/www -name "database_daily_*.sqlite" -mtime +7 -delete
```

## 🔍 故障排除

### 常见问题

#### 1. Nginx启动失败
```bash
# 检查配置语法
sudo nginx -t

# 查看错误日志
sudo tail -20 /var/log/nginx/error.log
```

#### 2. Node.js服务启动失败
```bash
# 查看PM2日志
pm2 logs douyin-admin

# 检查端口占用
sudo lsof -i :3000
```

#### 3. 数据库连接失败
```bash
# 检查SQLite文件权限
ls -la /var/www/douyin-admin-master/database.sqlite

# 测试数据库连接
cd /var/www/douyin-admin-master
node -e "require('./config/database').testConnection()"
```

#### 4. 前端访问异常
```bash
# 检查Nginx配置
sudo nginx -T | grep -A 10 "server_name your-domain.com"

# 检查前端文件
ls -la /var/www/html/index.html
```

### 快速恢复
```bash
# 重启所有服务
sudo systemctl restart nginx
pm2 restart all

# 检查状态
pm2 status
sudo systemctl status nginx
```

## 📞 技术支持

### 验证部署成功
```bash
# 1. 检查服务状态
pm2 status
sudo systemctl status nginx

# 2. 测试API
curl https://your-domain.com/api/health

# 3. 测试前端
curl -I https://your-domain.com

# 4. 检查数据库
cd /var/www/douyin-admin-master
node scripts/show-db-data.js | head -20
```

### 紧急联系
如遇严重问题：
1. 查看服务日志：`pm2 logs douyin-admin`
2. 检查系统日志：`sudo journalctl -u nginx -n 50`
3. 验证配置文件：`sudo nginx -t`
4. 联系技术支持团队

---

## 📝 部署清单

- [ ] 服务器准备和SSH配置
- [ ] 系统软件安装 (Node.js, Nginx, PM2)
- [ ] 项目代码部署
- [ ] 环境变量配置
- [ ] 数据库初始化
- [ ] Nginx配置和SSL证书
- [ ] 前端构建和部署
- [ ] 后端服务启动
- [ ] 监控和维护脚本设置
- [ ] 功能测试和验证

**🎉 部署完成后，访问 https://your-domain.com 即可使用系统！**